// EXERCÍCIO 03 | MÓDULO 1
// Objetivo: criar uma variável de cada tipo primitivo e conferir o tipo com typeof.

// Tarefa 1: declare uma const chamada "nomeAluno" com um texto (string).

const nomeAluno = String
// Tarefa 2: declare uma const chamada "notaFinal" com um número (number).
const notaFinal = Number

// Tarefa 3: declare uma const chamada "estaAprovado" com true ou false (boolean).
const estaAprovado = Boolean

// Tarefa 4: declare uma variável com let chamada "apelido", SEM atribuir valor
//         (ela deve ficar undefined).
let apelido = undefined

// Tarefa 5: declare uma const chamada "responsavelPresente" com o valor null.
const responsavelPresente = null

// Tarefa 6: use console.log(typeof ...) para mostrar o tipo de CADA UMA das cinco
//         variáveis acima (cinco linhas de console.log).
console.log(typeof nomeAluno);
console.log(typeof notaFinal);
console.log(typeof estaAprovado);
console.log(typeof apelido);
console.log(typeof responsavelPresente);






// Tarefa 7: em um comentário, explique por que typeof null mostra "object" mesmo
//         null não sendo, de fato, um objeto comum.

