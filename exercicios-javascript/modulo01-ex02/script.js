// EXERCÍCIO 02 | MÓDULO 1
// Objetivo: praticar a diferença entre let (reatribuível) e const (fixa).

// Tarefa 1: declare uma variável com let chamada "pontuacao", começando em 0.
let pontuacao = 0;

// Tarefa 2: reatribua "pontuacao" somando 10 (pontuacao = pontuacao + 10;).

pontuacao = pontuacao = 10;
// Tarefa 3: reatribua "pontuacao" somando mais 5.
pontuacao = pontuacao + 5;

// Tarefa 4: use console.log() para mostrar o valor final de "pontuacao".

console.log(pontuacao);

// Tarefa 5: declare uma const chamada "nomeDaEscola" com o valor "CEDTEC".
//         Em seguida, DESCOMENTE a linha abaixo (tire o //) e execute o script.
//nomeDaEscola = "Outra escola";
const nomeDaEscola = "CEDTEC";
// Tarefa 6: leia a mensagem de erro que apareceu no console e explique, em um
//         comentário aqui embaixo, por que ela aconteceu. Depois, comente
//         de novo a linha da Tarefa 5 para o restante do arquivo voltar a funcionar.
// pois em vez do let usou o const

